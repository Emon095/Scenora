import Image from "next/image";
import Link from "next/link";
import { Search, SlidersHorizontal } from "lucide-react";
import { Shell } from "@/components/Shell";
import { genres, movies, posts } from "@/data/demo";
import { ReviewPost } from "@/components/ReviewPost";
export default function Explore(){return <Shell><h1 className="page-title">Explore</h1><p className="subtitle">Discover movies, genres and reviews</p><div className="card" style={{display:"flex",alignItems:"center",padding:"4px 12px"}}><Search className="muted"/><input className="field" style={{border:0}} placeholder="Search movies, people, reviews..."/><SlidersHorizontal className="orange"/><b className="orange">Genres</b></div><div className="section-head"><h2>Browse by Genre</h2><span className="orange">See All</span></div><div className="genre-grid">{genres.map((g,i)=><Link className="card genre-card" style={{backgroundImage:`url(${movies[i%movies.length].backdrop})`}} href={`/explore/genre/${g.toLowerCase().replace("-","")}`} key={g}><b>{g}</b><small>{427+i*83} Movies</small></Link>)}</div><div className="section-head"><h2>Trending Movies</h2><Link href="/movies/popular" className="orange">See All</Link></div><div className="poster-row">{movies.map(m=><Link className="poster-card" href={`/movie/${m.id}`} key={m.id}><Image src={m.poster} width={180} height={270} alt={m.title}/><b>{m.title}</b><span className="orange">★ {m.rating}</span></Link>)}</div><div className="section-head"><h2>Top Reviews This Week</h2><span className="orange">See All</span></div><ReviewPost post={posts[0]}/></Shell>}
