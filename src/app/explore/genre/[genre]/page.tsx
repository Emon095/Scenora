import { Shell } from "@/components/Shell";import { ReviewPost } from "@/components/ReviewPost";import { posts } from "@/data/demo";
export default async function GenrePage({params}:{params:Promise<{genre:string}>}){const {genre}=await params;const title=genre.charAt(0).toUpperCase()+genre.slice(1);return <Shell back><h1 className="page-title">{title}</h1><p className="subtitle">1.2K Movies</p><div className="tabs"><button className="pill active">Movies</button><button className="pill">Series</button><button className="pill">Top⌄</button><button className="pill">Filter</button></div>{[...posts,...posts].map((p,i)=><ReviewPost post={{...p,id:p.id+i}} key={i}/>)}</Shell>}
