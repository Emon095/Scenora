import { Shell } from "@/components/Shell";import { MovieList } from "@/components/MovieList";import { movies } from "@/data/demo";
export default async function Country({params}:{params:Promise<{country:string}>}){const {country}=await params;const name=country.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");return <Shell back><h1 className="page-title">Country & Language</h1><p className="subtitle">Discover top rated movies from around the world</p><div className="tabs"><button className="pill active">🌐 By Country</button><button className="pill">By Language</button></div><div className="tabs" style={{marginTop:18}}>{["🇰🇷 South Korea","🇧🇩 Bangladesh","🇺🇸 USA","🇮🇳 India","🇯🇵 Japan","🇬🇧 UK"].map((x,i)=><button className={`pill ${i===0?"active":""}`} key={x}>{x}</button>)}</div><div className="section-head"><h2>Top Rated in {name}</h2><button className="pill">Highest Rated⌄</button></div><MovieList items={[movies[3],movies[1],movies[0],movies[4]]}/></Shell>}
